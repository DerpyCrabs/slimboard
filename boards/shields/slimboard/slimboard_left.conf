# Disable Battery Service (not needed)
CONFIG_BT_BAS=n

# Disable all sleep modes to prevent immediate shutdown
CONFIG_ZMK_SLEEP=n

# Prevent deep sleep states
CONFIG_PM_DEVICE=n

# Increase BLE connection stability
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
